'use client';
export default function ImportPage() { return <div><h1 className="text-3xl font-bold">Question Import</h1><p className="mt-3 text-gray-600">Bulk import is not configured for the current database schema. Add questions through the Questions page using direct option fields.</p></div>; }
