// Middleware removed - using client-side authentication only
// All authentication is handled by AuthProvider context

export async function middleware() {
  return;
}

export const config = {
  matcher: [],
};